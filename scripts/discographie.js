const swiper = new Swiper('.headerDisco .swiper-container', {
    effect: 'fade',
    direction: 'vertical',
    loop: true,
    autoplay: {
        delay: 2000,
        disableOnInteraction: false,
    }
})
